#input을 위한 함수, Java의 scanner와 유사
name = input("What's your name?")
print(len(name))
print(len(name.replace(' ','')))
#len 글자수 카운트 함수

# python은 변수 이름에서 cammel case를 사용하지 않는다. / snake case 사용 / 가장 앞에 숫자가 올수 없다.

name_of_user = "충환"